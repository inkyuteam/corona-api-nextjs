// theme/themeConfig.ts
import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  components: {
    Divider : {
        margin : 10,
        marginLG : 16
    },
    Button : {
      defaultBg : '#ffffff',
    }
  },

};

export default theme;